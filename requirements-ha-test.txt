# Match the actual HA runtime and Bluetooth/USB manifests used by these tests.
# Separate from the Python 3.13 standalone client's Bleak dependency.
pytest-homeassistant-custom-component==0.13.366
bleak-retry-connector==4.7.0
habluetooth==6.26.11
aiousbwatcher==1.1.2
serialx==1.10.0
dbus-fast==5.0.22
